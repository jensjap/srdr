<div id="validation_message_adverse_events">
<%= render "layouts/modal_info_messages" %>
</div>

<%= form_for @extraction_form_adverse_event, :remote=>true do |f|%>
	<%= hidden_field_tag "extraction_form_adverse_event[extraction_form_id]", @extraction_form.id %>
	<%= hidden_field_tag "extraction_form_id", @extraction_form.id %>
	<%= hidden_field_tag "project_id", @project.id %>

		<table class='form_table tab-list-table'>
			<tr>
				<td class='label top'>
			<label for="Adverse Event title">Adverse Event Title:</label>
				</td>
				<td class='input top'>
			<%= f.text_field "title", :class => "editable_field", :tabindex => 1, :title => "title" %>
				</td>
			</tr>
			
			<tr>
				<td class='label'>
			<label for="Adverse Event description">Adverse Event Description:</label>
				</td>
				<td class='input'>
			<%= f.text_area "description", :cols => 50, :rows => 4, :class => "editable_field", :tabindex => 2, :title => "description" %>
				</td>
			</tr>
		</table>
		<!--<div class='field'>
			User Note:<br/>
			<%# f.text_area "note", :cols => 50, :rows => 1, :class => "editable_field" %>
		</div> -->
		<br clear='all'/><br/><br/><br/><br/>
		<div class='popup-footer actions'>
			<% if @editing %>
				<button type='submit' class='positive' data-disable-with="<%= ajax_wait_msg %>" tabindex="3">
					<%= image_tag "Add.png", :alt => "Add"  %>Save Edits
				</button>
			<% else %>
				<button type='submit' class='positive' data-disable-with="<%= ajax_wait_msg %>" tabindex="3">
					<%= image_tag "Add.png", :alt => "Add"  %>Save
				</button>
			<% end %>
				<button class="negative" id="add_adverse_event_close" tabindex="4">
					<%= image_tag "Cancel.png", :alt => "Cancel" %>Cancel
				</button>
		</div>

<% end %>	

<script type="text/javascript">

$("#add_adverse_event_close").bind("click", function(event)
{
event.preventDefault();
$('#new_adverse_event_title_div').dialog('close');
});

</script>