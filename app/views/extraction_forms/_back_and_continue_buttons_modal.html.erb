<%
# determine if the page rendering this is the first or last in the tab list, 
# telling us whether or not to render the next or back buttons. These variables
# are passed into the :locals hash
first = first.nil? ? false : first
last = last.nil? ? false : last
top = top.nil? ? false : top
style = top ? "float:right;margin-right:10px;" : "width:100%;margin-left:5px;margin-right:10px;" 
%>

<% unless top %>
	<br/><div class='hr'></div><br/>
	<center>
<% end %>
	<table class="bottom_nav" style="<%= style %>" id="back_and_continue_buttons_modal" summary="back and continue buttons for modal window">
	<tr>
		<td align="left">
			<% unless first %>
			<div class='actions'>
			<a href="#" class="page_flip_btn_bk_mod">
  			<img src="/images/Silk_bullet_back.png" style="border:0px; vertical-align:text-top" alt="go back"/>
				Previous
  			</a>
  			</div>
  			<% end %>
		</td>
		<td align="right">
			<% unless last %>
			<div class='actions'>
			<a href="#" class="page_flip_btn_fw_mod">
  			Next
  			<img src="/images/Silk_bullet_go.png" style="border:0px; vertical-align:text-top" alt="continue"/>
  			</a>
  			</div>
  			<% end %>
  		</td>
	</tr>
</table>
<% unless top %></center><%end %>
<br/><br/>
<script type="text/javascript">
	$(".page_flip_btn_bk_mod").unbind("click");
	$(".page_flip_btn_bk_mod").bind("click", function(e){
		e.preventDefault();
		var total_tabs = $('#tabs').tabs("length");
		var selected =$('#tabs').tabs('option', 'selected');
		if ((selected > 0) && (selected < total_tabs))
		{
			$('#tabs').tabs('select', selected-1);
		}
		else if (selected == 0)
		{
		}
		else if (selected >= total_tabs)
		{
		
		}
	});
	$(".page_flip_btn_fw_mod").unbind("click");
	$(".page_flip_btn_fw_mod").bind("click", function(e){
		e.preventDefault();
		var total_tabs = $('#tabs').tabs("length");
		var selected =$('#tabs').tabs('option', 'selected');
		if ((selected > 0) && (selected < total_tabs))
		{
			$('#tabs').tabs('select', selected+1);
		}
		else if(selected == 0)
		{
			$("#tabs").tabs('select',1);
		}
		else if (selected >= total_tabs)
		{
		
		}
	});	
	
	</script>