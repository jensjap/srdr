var str = "<%= @has_study_data %>";
var has_study_data = (str === 'true');

if(has_study_data){
	var message = "One or more studies have been created that use this data item. Changing this data item will change it for these studies as well. Are you sure you want to continue?";
	if (confirm(message)){
		$("#<%= @model %>_fields_entry").html("<%= escape_javascript(render :partial=>'question_builder/edit_form') %>");
		$("#<%= @model %>_fields_entry").dialog("option", "title", "Editing <%= @model_title %>");
		$("#<%= @model %>_fields_entry :button").removeAttr("disabled");
		$("#<%= @model %>_fields_entry").dialog("option","modal",true);
		$("#<%= @model %>_fields_entry").dialog("open");
	}
}else{
	$("#<%= @model %>_fields_entry").html("<%= escape_javascript(render :partial=>'question_builder/edit_form') %>");
	$("#<%= @model %>_fields_entry").dialog("option", "title", "Editing <%= @model_title %>");
	$("#<%= @model %>_fields_entry :button").removeAttr("disabled");
	$("#<%= @model %>_fields_entry").dialog("option","modal",true);
	$("#<%= @model %>_fields_entry").dialog("open");
}
$("#waitingGIF").remove();